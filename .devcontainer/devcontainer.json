{
	"name": "opencv_cuda",
	"build": {
        "dockerfile": "../.docker/Dockerfile"
    },
	"customizations": {
		"vscode": {
			"extensions": [
				"donjayamanne.python-extension-pack"
			]
		}
	},
    // additional mount
    // "mounts": [
    //     "source=/path/to/host,target=/path/to/container,type=bind"
    // ],
	"runArgs": ["--gpus", "all"]
}